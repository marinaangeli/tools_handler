<div class="container">
  <div class="class">
    <h1>INDEX TOOLS</h1>
  </div>


  <div class="tools-grid">

    <% @tools.each do |tool| %>
      <div class="card shadow">
        <img class="card-img-top cover" src="https://source.unsplash.com/random/300×150/?<%= tool.name.gsub(" ","-")%>)">
        <h6 class="card-title text-center fw-semibold p-1"><%= tool.name.capitalize %></h6>
        <div class="d-fex">
          <span class="position-absolute bottom-0 start-0 m-1 ms-2 p-1">$<%= tool.price%></span>
          <%= link_to "Rent now", tool_path(tool), class:"btn-yellow position-absolute bottom-0 end-0" %>
        </div>
      </div>
    <% end %>
  </div>

</div>
